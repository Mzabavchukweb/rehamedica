'use strict';
// === INDEKS WYSZUKIWARKI (auto-generowany ze stron site/**/index.html; pomija noindex) ===
// Zasilany do window.RM_SEARCH; obsługiwany przez IIFE 'wyszukiwarka po całym serwisie' niżej.
window.RM_SEARCH=[{"u":"aktualnosci/cialo-bez-kompleksow/","t":"Ciało bez kompleksów","c":"Aktualności","k":"Ciało bez kompleksów Głównym celem Programu jest wzmocnienie roli i umiejętności opiekunów dzieci onkologicznych, które przyczyniają się do poprawy ich jakości życia w…"},{"u":"aktualnosci/","t":"Aktualności","c":"Aktualności","k":"Aktualności Reha Medica | Szczecinek Bieżące informacje Reha Medica: współpraca z Medicover, program Innowacja Model DOM i inne wiadomości z placówek."},{"u":"aktualnosci/reha-medica-dolacza-do-programu-innowacja-model-dom/","t":"Reha Medica dołącza do programu Innowacja „Model DOM”","c":"Aktualności","k":"Reha Medica dołącza do programu Innowacja „Model DOM” | Reha Wydarzenie zorganizowane zostało przez Ośrodek Środowiskowej Opieki Psychologicznej i Psychoterapeutycznej w Szczecinku i jest częścią projektu: „…"},{"u":"aktualnosci/wspolpraca-reha-medica-z-medicover/","t":"Współpraca Reha Medica z MEDICOVER","c":"Aktualności","k":"Współpraca Reha Medica z MEDICOVER Reha Medica to doświadczeni, wysoko wykwalifikowani fizjoterapeuci i terapeuci manualni, właściwe diagnozowanie, skuteczne, indywidualnie…"},{"u":"ankieta-satysfakcji/","t":"Ankieta satysfakcji pacjenta","c":"Ankieta","k":"Ankieta satysfakcji pacjenta Anonimowa ankieta satysfakcji pacjenta Reha Medica. Trzynaście pytań o rejestrację, wizytę, personel i placówkę. Odpowiedzi pomagają podnosić jakość opieki."},{"u":"faq/","t":"Najczęstsze pytania","c":"FAQ","k":"FAQ Reha Medica | skierowania, pierwsza wizyta Odpowiedzi na najczęstsze pytania: jak przygotować się do wizyty, kto wystawia skierowanie i jak długo jest ważne."},{"u":"","t":"Centrum rehabilitacyjno-medyczne","c":"Strona główna","k":"Reha Medica Szczecinek — centrum rehabilitacyjno-medyczne Centrum rehabilitacyjno-medyczne Reha Medica. Rehabilitacja, fizjoterapia, konsultacje i zabiegi w pięciu placówkach na Pomorzu Zachodnim. Umów wizytę."},{"u":"kontakt/","t":"Kontakt","c":"Kontakt","k":"Kontakt Kontakt do wszystkich placówek Reha Medica: adresy, telefony i adresy e-mail w Szczecinku, Szczecinie, Wałczu, Białogardzie i Bobolicach."},{"u":"o-nas/","t":"O Reha Medica","c":"O nas","k":"O nas Reha Medica to centrum rehabilitacyjno-medyczne działające w pięciu placówkach na Pomorzu Zachodnim. Zespół lekarzy, fizjoterapeutów i logopedów."},{"u":"placowki/","t":"Placówki","c":"Placówki","k":"Placówki Reha Medica | Szczecinek, Szczecin, Wałcz Pięć placówek Reha Medica na Pomorzu Zachodnim: Szczecinek, Szczecin, Wałcz, Białogard i Bobolice. Adresy, telefony i godziny otwarcia."},{"u":"placowki/walcz/","t":"Reha Medica Wałcz","c":"Placówki","k":"Reha Medica Wałcz | rehabilitacja domowa NFZ Reha Medica Wałcz, ul. Gen. Andersa 9: rehabilitacja domowa w ramach NFZ. Telefon, e-mail i godziny otwarcia. Umów wizytę w recepcji placówki."},{"u":"polityka-prywatnosci/","t":"Polityka prywatności","c":"Informacje","k":"Polityka prywatności Zasady przetwarzania danych osobowych pacjentów Reha Medica, podstawy prawne, okresy przechowywania i prawa osób, których dane dotyczą."},{"u":"projekty-ue/","t":"Aktualności – projekty","c":"Projekty UE","k":"Projekty UE i dofinansowania Aktualności z projektów realizowanych przez Reha Medica z dofinansowaniem Unii Europejskiej, w tym projekt MINDSpots."},{"u":"projekty-ue/projekt-mindspots/","t":"Projekt pt. „MINDSpots – Rozwój Ośrodków Opieki Psychologicznej i Psychoterapeutycznej w województwie zachodniopomorskim”","c":"Projekty UE","k":"Projekt MINDSpots Projekt MINDSpots: rozwój ośrodków opieki psychologicznej i psychoterapeutycznej w województwie zachodniopomorskim, ze środków EFS+."},{"u":"projekty-ue/zakonczenie-postepowania-ofertowego-komputery-mindspots/","t":"Zakończenie postępowania ofertowego na zakup komputerów – Projekt „MINDSpots”","c":"Projekty UE","k":"Zakończenie postępowania ofertowego Zakończenie postępowania ofertowego na zakup laptopów w projekcie MINDSpots. Nie wyłoniono wykonawcy, oferty przekroczyły budżet."},{"u":"standardy-ochrony-dzieci/","t":"Standardy ochrony dzieci","c":"Informacje","k":"Standardy ochrony małoletnich Standardy ochrony małoletnich obowiązujące w placówkach Reha Medica: zasady, procedury zgłaszania i ochrona dzieci."},{"u":"uslugi/","t":"Usługi","c":"Usługi","k":"Usługi Reha Medica Szczecinek | rehabilitacja i fizjoterapia Usługi Reha Medica w Szczecinku: rehabilitacja i fizjoterapia, konsultacje specjalistyczne, zabiegi fizykoterapeutyczne, masaże i odnowa biologiczna."},{"u":"uslugi/konsultacje/fizjoterapeutyczna/","t":"Konsultacja fizjoterapeutyczna w Szczecinku","c":"Konsultacje","k":"Konsultacja fizjoterapeutyczna Szczecinek Konsultacja fizjoterapeutyczna w Szczecinku: badanie kręgosłupa przy nawracającym bólu, wykrywanie wad postawy u dzieci, przygotowanie do aktywności."},{"u":"uslugi/konsultacje/","t":"Konsultacje specjalistyczne","c":"Konsultacje","k":"Konsultacje specjalistyczne Szczecinek Konsultacje fizjoterapeutyczne, ortopedyczne, neurologiczne oraz logopedyczne i neurologopedyczne w Reha Medica w Szczecinku."},{"u":"uslugi/konsultacje/logopedyczna-neurologopedyczna/","t":"Konsultacja logopedyczna / neurologopedyczna w Szczecinku","c":"Konsultacje","k":"Konsultacja logopedyczna Szczecinek Logopeda i neurologopeda w Szczecinku — konsultacja dla dzieci i dorosłych, także przy trudnościach wynikających z dysfunkcji neurologicznych."},{"u":"uslugi/konsultacje/neurologiczna/","t":"Konsultacja neurologiczna w Szczecinku","c":"Konsultacje","k":"Konsultacja neurologiczna Szczecinek Neurolog w Szczecinku — konsultacja przy zawrotach i bólach głowy, zaburzeniach równowagi oraz dolegliwościach neurologicznych po przebytym COVID-19."},{"u":"uslugi/konsultacje/ortopedyczna/","t":"Konsultacja ortopedyczna w Szczecinku","c":"Konsultacje","k":"Konsultacja ortopedyczna Szczecinek Ortopeda w Szczecinku — konsultacja przy urazach i kontuzjach narządu ruchu, bólu kręgosłupa, drętwieniu kończyn i ograniczonej ruchomości stawów."},{"u":"uslugi/konsultacje/socjologiczna/","t":"Konsultacja socjologiczna w Szczecinku","c":"Konsultacje","k":"Konsultacja socjologiczna Szczecinek Konsultacja socjologiczna w Szczecinku: socjoterapia przy trudnościach w kontaktach społecznych, nieśmiałości, wycofaniu i niedostosowaniu społecznym."},{"u":"uslugi/masaze/","t":"Masaże lecznicze","c":"Masaże","k":"Masaże lecznicze Szczecinek Masaże lecznicze w Szczecinku: masaż leczniczy, sportowy, pneumatyczny oraz manualny drenaż limfatyczny w Reha Medica."},{"u":"uslugi/masaze/manualny-drenaz-limfatyczny/","t":"Manualny drenaż limfatyczny w Szczecinku","c":"Masaże","k":"Manualny drenaż limfatyczny Szczecinek Manualny drenaż limfatyczny w Szczecinku: usprawnia przepływ krwi i limfy, redukuje obrzęki, niweluje ból i pomaga w walce z cellulitem."},{"u":"uslugi/masaze/masaz-leczniczy/","t":"Masaż leczniczy w Szczecinku","c":"Masaże","k":"Masaż leczniczy Szczecinek Masaż leczniczy w Szczecinku: ręczna stymulacja tkanek wykonywana na obszarze ciała objętym chorobą lub dolegliwością bólową."},{"u":"uslugi/masaze/masaz-modelujacy-antycellulitowy/","t":"Masaż modelujący, antycellulitowy w Szczecinku","c":"Masaże","k":"Masaż antycellulitowy Szczecinek Masaż modelujący i antycellulitowy w Szczecinku: intensywne techniki wspierające redukcję cellulitu i spalanie tkanki tłuszczowej."},{"u":"uslugi/masaze/masaz-pneumatyczny/","t":"Masaż pneumatyczny w Szczecinku","c":"Masaże","k":"Masaż pneumatyczny Szczecinek Masaż pneumatyczny w Szczecinku: drenaż limfatyczny wykonywany sprężonym powietrzem w urządzeniu BOA, wspierający redukcję obrzęków."},{"u":"uslugi/masaze/masaz-sportowy/","t":"Masaż sportowy w Szczecinku","c":"Masaże","k":"Masaż sportowy Szczecinek Masaż sportowy w Szczecinku: intensywniejszy niż klasyczny, z elementami masażu izometrycznego wzmacniającego osłabione partie mięśni."},{"u":"uslugi/odnowa-biologiczna/","t":"Odnowa biologiczna","c":"Odnowa biologiczna","k":"Odnowa biologiczna Szczecinek Odnowa biologiczna w Szczecinku: sauna sucha i infrared, kriokomora oraz masaże relaksacyjne w Reha Medica. Regeneracja i relaks w jednym miejscu."},{"u":"uslugi/odnowa-biologiczna/kriokomora/","t":"Kriokomora w Szczecinku","c":"Odnowa biologiczna","k":"Kriokomora Szczecinek Kriokomora w Szczecinku, czyli krioterapia ogólnoustrojowa (całego ciała): zapalenia okołostawowe, przykurcze mięśniowe, mikrourazy i stłuczenia."},{"u":"uslugi/odnowa-biologiczna/masaze-relaksacyjne/","t":"Masaże relaksacyjne","c":"Odnowa biologiczna","k":"Masaże relaksacyjne Szczecinek — rodzaje Masaże relaksacyjne w Szczecinku: gorącymi kamieniami, olejkiem arganowym, masłem shea, peelingujący, antycellulitowy, twarzy i dla dwojga."},{"u":"uslugi/odnowa-biologiczna/masaze-relaksacyjne/masaz-goracymi-kamieniami/","t":"Masaż gorącymi kamieniami w Szczecinku","c":"Odnowa biologiczna","k":"Masaż gorącymi kamieniami Szczecinek Masaż gorącymi kamieniami w Szczecinku: tradycyjne techniki masażu z rozgrzanymi kamieniami. Masaż holistyczny, nazywany także uzdrawiającym."},{"u":"uslugi/odnowa-biologiczna/masaze-relaksacyjne/masaz-maslem-shea/","t":"Masaż masłem shea w Szczecinku","c":"Odnowa biologiczna","k":"Masaż masłem shea Szczecinek Masaż masłem shea w Szczecinku: silnie odprężający zabieg z aromaterapią i muzyką relaksacyjną, doceniany za właściwości pielęgnacyjne."},{"u":"uslugi/odnowa-biologiczna/masaze-relaksacyjne/masaz-olejkiem-arganowym-na-cieplo/","t":"Masaż olejkiem arganowym na ciepło w Szczecinku","c":"Odnowa biologiczna","k":"Masaż olejkiem na ciepło Szczecinek Masaż ciepłym olejem arganowym w Szczecinku: głęboko relaksuje, odpręża i przywraca energię. Rytuał dla ciała, duszy i umysłu."},{"u":"uslugi/odnowa-biologiczna/masaze-relaksacyjne/masaz-olejkiem-arganowym/","t":"Masaż olejkiem arganowym w Szczecinku","c":"Odnowa biologiczna","k":"Masaż olejkiem arganowym Szczecinek Masaż olejkiem arganowym w Szczecinku: pobudza krążenie, odżywia i odbudowuje skórę, korzystnie wpływa na stawy i kości."},{"u":"uslugi/odnowa-biologiczna/masaze-relaksacyjne/masaz-peelingujacy/","t":"Masaż peelingujący w Szczecinku","c":"Odnowa biologiczna","k":"Masaż peelingujący Szczecinek Masaż peelingujący w Szczecinku: połączenie masażu relaksacyjnego z peelingiem usuwającym martwe komórki naskórka i zanieczyszczenia."},{"u":"uslugi/odnowa-biologiczna/masaze-relaksacyjne/masaz-relaksacyjny-dla-dwojga/","t":"Masaż relaksacyjny dla dwojga w Szczecinku","c":"Odnowa biologiczna","k":"Masaż dla dwojga Szczecinek Masaż relaksacyjny dla dwojga w Szczecinku: kojący dotyk i delikatna muzyka w towarzystwie drugiej osoby. Odskocznia od stresu i codziennej gonitwy."},{"u":"uslugi/odnowa-biologiczna/masaze-relaksacyjne/masaz-relaksacyjny/","t":"Masaż relaksacyjny w Szczecinku","c":"Odnowa biologiczna","k":"Masaż relaksacyjny Szczecinek Masaż relaksacyjny w Szczecinku: uspokaja układ nerwowy, ujędrnia ciało, wzmacnia mięśnie i usprawnia krążenie."},{"u":"uslugi/odnowa-biologiczna/masaze-relaksacyjne/masaz-twarzy/","t":"Masaż twarzy w Szczecinku","c":"Odnowa biologiczna","k":"Masaż twarzy Szczecinek Masaż twarzy w Szczecinku: masaż klasyczny na specjalnym kremie, pobudza mikrokrążenie i dotlenia skórę. Polecany osobom zapracowanym."},{"u":"uslugi/odnowa-biologiczna/sauny/","t":"Sauny","c":"Odnowa biologiczna","k":"Sauny Szczecinek Sauny w Szczecinku: sauna sucha (fińska) do 90–100°C oraz sauna infrared na podczerwień w Reha Medica. Rozgrzej mięśnie i zregeneruj się."},{"u":"uslugi/odnowa-biologiczna/sauny/sauna-infrared/","t":"Sauna Infrared w Szczecinku","c":"Odnowa biologiczna","k":"Sauna Infrared Szczecinek Sauna infrared w Szczecinku: ciepło z promieniowania podczerwonego przy napięciu mięśni, bólach kręgosłupa, rwie kulszowej i zwyrodnieniach stawów."},{"u":"uslugi/odnowa-biologiczna/sauny/sauna-sucha/","t":"Sauna sucha (fińska) w Szczecinku","c":"Odnowa biologiczna","k":"Sauna sucha (fińska) Szczecinek Sauna sucha (fińska) w Szczecinku: temperatura do 90-100°C przy niskiej wilgotności. Napięcie mięśni, zwyrodnienia stawów, ból zatok."},{"u":"uslugi/poradnia-logopedyczna/","t":"Poradnia logopedyczna w Szczecinku","c":"Usługi","k":"Poradnia logopedyczna Szczecinek Logopeda w Szczecinku — poradnia logopedyczna. Diagnostyka i terapia zaburzeń mowy, wad wymowy, jąkania i dysleksji. Wskazania przed wizytą."},{"u":"uslugi/rehabilitacja/gabinet-terapii-manualnej/","t":"Gabinet terapii manualnej w Szczecinku","c":"Rehabilitacja","k":"Gabinet terapii manualnej Szczecinek Terapia manualna w Szczecinku przy schorzeniach kręgosłupa, zwyrodnieniach stawów, dysfunkcjach mięśniowych i powięziowych oraz ograniczeniu ruchu."},{"u":"uslugi/rehabilitacja/","t":"Rehabilitacja","c":"Rehabilitacja","k":"Rehabilitacja Szczecinek Rehabilitacja w Szczecinku: poradnia rehabilitacyjna, ośrodek dzienny, rehabilitacja lecznicza, domowa oraz dla dzieci i młodzieży."},{"u":"uslugi/rehabilitacja/osrodek-rehabilitacji-ogolnoustrojowej/","t":"Ośrodek rehabilitacji ogólnoustrojowej w Szczecinku","c":"Rehabilitacja","k":"Rehabilitacja ogólnoustrojowa NFZ Szczecinek Ośrodek rehabilitacji ogólnoustrojowej w Szczecinku, w ramach NFZ. Rehabilitacja na oddziale dziennym od 15 do 30 dni zabiegowych. Skierowanie i wskazania."},{"u":"uslugi/rehabilitacja/poradnia-rehabilitacyjna/","t":"Poradnia rehabilitacyjna w Szczecinku","c":"Rehabilitacja","k":"Poradnia rehabilitacyjna Szczecinek Poradnia rehabilitacyjna w Szczecinku. Diagnostyka i leczenie schorzeń ortopedycznych, reumatologicznych i neurologicznych. Wskazania i wymagania."},{"u":"uslugi/rehabilitacja/rehabilitacja-bolu/","t":"Rehabilitacja bólu w Szczecinku","c":"Rehabilitacja","k":"Rehabilitacja bólu Szczecinek Rehabilitacja bólu w Szczecinku: ból kręgosłupa, rwa kulszowa i ramienna, migreny oraz ból stawu skroniowo-żuchwowego. Zabiegi dobierane indywidualnie."},{"u":"uslugi/rehabilitacja/rehabilitacja-domowa/","t":"Rehabilitacja domowa w Szczecinku i Wałczu","c":"Rehabilitacja","k":"Rehabilitacja domowa Szczecinek i Wałcz Rehabilitacja w warunkach domowych w Szczecinku i Wałczu, w ramach umów z NFZ. Dla pacjentów, którzy nie mogą korzystać z rehabilitacji ambulatoryjnej."},{"u":"uslugi/rehabilitacja/rehabilitacja-dzieci-i-mlodziezy/","t":"Rehabilitacja dzieci i młodzieży w Szczecinku","c":"Rehabilitacja","k":"Rehabilitacja dzieci i młodzieży Szczecinek Rehabilitacja dzieci i młodzieży w Szczecinku. Wady postawy, kręgosłupa, stóp i kolan. Metody NDT-Bobath, Vojty i Peto. Wskazania i efekty."},{"u":"uslugi/rehabilitacja/rehabilitacja-lecznicza/","t":"Rehabilitacja lecznicza","c":"Rehabilitacja","k":"Rehabilitacja lecznicza Szczecinek Rehabilitacja ortopedyczna, neurologiczna, reumatologiczna i sportowa, rehabilitacja bólu oraz gabinet terapii manualnej w Szczecinku."},{"u":"uslugi/rehabilitacja/rehabilitacja-neurologiczna/","t":"Rehabilitacja neurologiczna w Szczecinku","c":"Rehabilitacja","k":"Rehabilitacja neurologiczna Szczecinek Rehabilitacja neurologiczna w Szczecinku po udarze mózgu, przy chorobie Parkinsona, alzheimerze i stwardnieniu rozsianym. Wskazania, efekty i metody."},{"u":"uslugi/rehabilitacja/rehabilitacja-ortopedyczna/","t":"Rehabilitacja ortopedyczna w Szczecinku","c":"Rehabilitacja","k":"Rehabilitacja ortopedyczna Szczecinek Rehabilitacja ortopedyczna w Szczecinku po urazach i kontuzjach narządu ruchu, przeciążeniach oraz przed zabiegiem operacyjnym i po nim."},{"u":"uslugi/rehabilitacja/rehabilitacja-reumatologiczna/","t":"Rehabilitacja reumatologiczna w Szczecinku","c":"Rehabilitacja","k":"Rehabilitacja reumatologiczna Szczecinek Rehabilitacja reumatologiczna w Szczecinku przy reumatoidalnym zapaleniu stawów, twardzinie układowej i innych schorzeniach reumatycznych."},{"u":"uslugi/rehabilitacja/rehabilitacja-sportowa/","t":"Rehabilitacja sportowa w Szczecinku","c":"Rehabilitacja","k":"Rehabilitacja sportowa Szczecinek Rehabilitacja sportowa w Szczecinku po kontuzjach: łokieć tenisisty i golfisty, urazy więzadeł kolana, naderwania mięśni, bóle bioder i barków."},{"u":"uslugi/trening-ems/","t":"Trening EMS w Szczecinku","c":"Usługi","k":"Trening EMS Szczecinek Trening EMS w Szczecinku — elektrostymulacja mięśni. 30 minut treningu, indywidualny plan i wywiad medyczny przed startem. Reha Medica, ul. Kościuszki 57."},{"u":"uslugi/zabiegi-fizykoterapeutyczne/elektroterapia/","t":"Elektroterapia w Szczecinku","c":"Zabiegi fizykoterapeutyczne","k":"Elektroterapia Szczecinek Elektroterapia w Szczecinku: leczenie prądami przy nerwobólach, bólach i przykurczach mięśni oraz stanach zapalnych tkanek miękkich."},{"u":"uslugi/zabiegi-fizykoterapeutyczne/fala-uderzeniowa/","t":"Fala uderzeniowa w Szczecinku","c":"Zabiegi fizykoterapeutyczne","k":"Fala uderzeniowa Szczecinek Fala uderzeniowa w Szczecinku przy przewlekłych chorobach ścięgien, ostrodze piętowej oraz łokciu tenisisty i golfisty. Wskazania i przeciwwskazania."},{"u":"uslugi/zabiegi-fizykoterapeutyczne/hydromasaze/","t":"Hydromasaże w Szczecinku","c":"Zabiegi fizykoterapeutyczne","k":"Hydromasaże Szczecinek Hydromasaż w Szczecinku: zabieg łagodzący bóle, rozluźniający mięśnie i stawy oraz wspierający regenerację i dobre samopoczucie."},{"u":"uslugi/zabiegi-fizykoterapeutyczne/hydroterapia/","t":"Hydroterapia w Szczecinku","c":"Zabiegi fizykoterapeutyczne","k":"Hydroterapia Szczecinek Hydroterapia w Szczecinku, czyli wodolecznictwo: terapeutyczne zastosowanie wody w kąpielach i zabiegach wspierających regenerację i leczenie."},{"u":"uslugi/zabiegi-fizykoterapeutyczne/","t":"Zabiegi fizykoterapeutyczne","c":"Zabiegi fizykoterapeutyczne","k":"Zabiegi fizykoterapeutyczne Szczecinek Zabiegi fizykoterapeutyczne w Szczecinku: elektroterapia, fala uderzeniowa, laseroterapia, magnetoterapia, ultradźwięki, hydroterapia i krioterapia."},{"u":"uslugi/zabiegi-fizykoterapeutyczne/krioterapia/","t":"Krioterapia w Szczecinku","c":"Zabiegi fizykoterapeutyczne","k":"Krioterapia Szczecinek Krioterapia w Szczecinku, czyli leczenie zimnem: obrzęki, ból stawów, stany zapalne i napięcie mięśniowe. Miejscowe schładzanie tkanek w fizykoterapii."},{"u":"uslugi/zabiegi-fizykoterapeutyczne/laseroterapia/","t":"Laseroterapia w Szczecinku","c":"Zabiegi fizykoterapeutyczne","k":"Laseroterapia Szczecinek Laseroterapia w Szczecinku przy stanach zapalnych tkanek miękkich i stawów oraz zespołach przeciążeniowych. Laser przyspiesza gojenie tkanek."},{"u":"uslugi/zabiegi-fizykoterapeutyczne/magnetoterapia/","t":"Magnetoterapia w Szczecinku","c":"Zabiegi fizykoterapeutyczne","k":"Magnetoterapia Szczecinek Magnetoterapia w Szczecinku: pole magnetyczne niskiej częstotliwości przy urazach, chorobach reumatycznych i patologiach narządu ruchu."},{"u":"uslugi/zabiegi-fizykoterapeutyczne/ultradzwieki/","t":"Ultradźwięki w Szczecinku","c":"Zabiegi fizykoterapeutyczne","k":"Ultradźwięki Szczecinek Ultradźwięki w Szczecinku przy zapaleniach stawów, mięśni i ścięgien, zmianach zwyrodnieniowych, nerwobólach oraz bliznach i bliznowcach."}];


// Ruch ograniczamy, gdy prosi o to system LUB gdy użytkownik zapisał to w menu dostępności.
// Dzięki temu przełącznik „Ogranicz ruch" realnie wyłącza autoodtwarzanie hero (po odświeżeniu).
var REDUCE = window.matchMedia('(prefers-reduced-motion: reduce)').matches || (function () {
  try { return !!JSON.parse(localStorage.getItem('rm-a11y') || '{}').reduce; } catch (e) { return false; }
})();

/* ---------- header: elegancki stan po scrollu ---------- */
(function () {
  var header = document.getElementById('site-header');
  if (!header) return;
  var ticking = false;
  function frame() {
    header.classList.toggle('is-scrolled', (window.pageYOffset || 0) > 36);
    ticking = false;
  }
  window.addEventListener('scroll', function () {
    if (!ticking) { requestAnimationFrame(frame); ticking = true; }
  }, { passive: true });
  frame();
})();

/* ---------- mobilne menu (przebudowane: nakładka, zamykanie X/Esc/link, blokada scrolla, fokus) ---------- */
(function () {
  var burger = document.querySelector('.burger');
  var nav = document.getElementById('nav');
  if (!burger || !nav) return;
  var closeBtn = nav.querySelector('[data-nav-close]');

  function focusables() {
    return Array.prototype.slice.call(
      nav.querySelectorAll('a[href],button:not([disabled])')
    ).filter(function (el) { return el.offsetWidth > 0 || el.offsetHeight > 0; });
  }
  function isOpen() { return nav.classList.contains('nav--open'); }

  function open() {
    nav.classList.add('nav--open');
    burger.setAttribute('aria-expanded', 'true');
    document.body.style.overflow = 'hidden';
    document.addEventListener('keydown', onKey);
    // Fokus dopiero po DWÓCH klatkach — inaczej zostaje na burgerze, a Tab ucieka
    // poza nakładkę do treści pod spodem.
    // Dlaczego aż tyle: .nav__close ma `transition:visibility .34s`, więc w chwili
    // dodania klasy .nav--open wartość to nadal `hidden` (przejście jest w t=0),
    // a focus() na elemencie visibility:hidden jest ignorowany. `visible` pojawia się
    // dopiero w kolejnej klatce. Zmierzone: po 1× rAF → hidden, po 2× rAF → visible.
    // Wymuszony reflow (offsetHeight) tu NIE pomaga — to nie kwestia przeliczenia stylów.
    var f = closeBtn || nav.querySelector('a[href]');
    if (f) requestAnimationFrame(function () { requestAnimationFrame(function () { f.focus(); }); });
  }
  function close(returnFocus) {
    nav.classList.remove('nav--open');
    burger.setAttribute('aria-expanded', 'false');
    document.body.style.overflow = '';
    document.removeEventListener('keydown', onKey);
    // zwiń otwarte akordeony submenu
    Array.prototype.forEach.call(nav.querySelectorAll('.nav__item--sub.is-open'), function (li) {
      li.classList.remove('is-open');
      var t = li.querySelector('.nav__top'); if (t) t.setAttribute('aria-expanded', 'false');
    });
    if (returnFocus !== false) burger.focus();
  }
  function onKey(e) {
    if (e.key === 'Escape') { e.preventDefault(); close(); return; }
    if (e.key === 'Tab') {
      var f = focusables(); if (!f.length) return;
      var first = f[0], last = f[f.length - 1];
      if (e.shiftKey && document.activeElement === first) { e.preventDefault(); last.focus(); }
      else if (!e.shiftKey && document.activeElement === last) { e.preventDefault(); first.focus(); }
    }
  }
  burger.addEventListener('click', function () { isOpen() ? close() : open(); });
  if (closeBtn) closeBtn.addEventListener('click', function () { close(); });
  nav.addEventListener('click', function (e) { if (e.target.closest('a')) close(false); });
  window.addEventListener('resize', function () {
    if (window.innerWidth > 1024 && isOpen()) close(false);
  });
})();

/* ---------- rozwijane submenu — otwieranie NA KLIK (desktop dropdown + mobile akordeon) ---------- */
(function () {
  var subs = Array.prototype.slice.call(document.querySelectorAll('.nav__item--sub'));
  if (!subs.length) return;
  function setOpen(li, open) {
    li.classList.toggle('is-open', open);
    var t = li.querySelector('.nav__top');
    if (t) t.setAttribute('aria-expanded', open ? 'true' : 'false');
  }
  function closeAll(except) { subs.forEach(function (li) { if (li !== except) setOpen(li, false); }); }
  subs.forEach(function (li) {
    var btn = li.querySelector('.nav__top');
    if (btn) btn.addEventListener('click', function (e) {
      e.preventDefault();
      var willOpen = !li.classList.contains('is-open');
      closeAll(li);             // tylko jedno otwarte naraz
      setOpen(li, willOpen);
    });
    // klik w pozycję submenu zamyka rozwinięcie
    Array.prototype.forEach.call(li.querySelectorAll('.nav__sub a'), function (a) {
      a.addEventListener('click', function () { setOpen(li, false); });
    });
  });
  // klik poza nawigacją zamyka wszystkie
  document.addEventListener('click', function (e) {
    if (!e.target.closest('.nav__item--sub')) closeAll(null);
  });
  document.addEventListener('keydown', function (e) { if (e.key === 'Escape') closeAll(null); });
})();

/* ---------- restrained active state (scrollspy) ---------- */
(function () {
  if (!('IntersectionObserver' in window)) return;
  var links = Array.prototype.slice.call(document.querySelectorAll('.nav ul a[href^="#"]'));
  var map = {};
  links.forEach(function (l) {
    var s = document.getElementById(l.getAttribute('href').slice(1));
    if (s) map[s.id] = l;
  });
  var ids = Object.keys(map);
  if (!ids.length) return;
  var obs = new IntersectionObserver(function (entries) {
    entries.forEach(function (en) {
      if (en.isIntersecting) {
        links.forEach(function (l) { l.removeAttribute('aria-current'); });
        map[en.target.id].setAttribute('aria-current', 'true');
      }
    });
  }, { rootMargin: '-45% 0px -50% 0px', threshold: 0 });
  ids.forEach(function (id) { obs.observe(document.getElementById(id)); });
})();

/* ---------- hero: pokaz slajdów (zdjęcie + tekst zmienia się co slajd) ---------- */
(function () {
  var hero = document.getElementById('hero');
  var slides = Array.prototype.slice.call(document.querySelectorAll('.hero__slide'));
  var dots = Array.prototype.slice.call(document.querySelectorAll('.hero__dot'));
  if (slides.length < 2) return;
  // H1 i podtytuł są STAŁE — jeden krótki, czytelny komunikat (zgodnie z referencją).
  // Ze slajdem zmienia się wyłącznie zdjęcie; tekst zostaje niezmienny.
  var i = 0, timer = null, userPaused = false, DELAY = 6000;
  if (dots[0]) dots[0].setAttribute('aria-current', 'true');

  function go(n) {
    n = (n + slides.length) % slides.length;
    if (n === i) return;
    slides[i].classList.remove('is-active');
    if (dots[i]) dots[i].removeAttribute('aria-current');
    i = n;
    slides[i].classList.add('is-active');
    if (dots[i]) dots[i].setAttribute('aria-current', 'true');
    // leniwe: dociągnij sąsiedni slajd dopiero, gdy jest potrzebny
    load(i); load((i + 1) % slides.length);
  }
  function load(n) {
    var s = slides[n];
    if (s && s.dataset.bg) { s.style.backgroundImage = "url('" + s.dataset.bg + "')"; delete s.dataset.bg; }
  }
  function start() { if (!REDUCE && !timer && !userPaused) timer = setInterval(function () { go(i + 1); }, DELAY); }
  function stop() { clearInterval(timer); timer = null; }
  dots.forEach(function (d, idx) {
    d.addEventListener('click', function () { userPaused = true; stop(); go(idx); });
  });
  // WCAG 2.2.2 — ruch zatrzymuje się, gdy użytkownik czyta hero (kursor / fokus klawiatury)
  if (hero) {
    hero.addEventListener('mouseenter', stop);
    hero.addEventListener('mouseleave', start);
    hero.addEventListener('focusin', stop);
    hero.addEventListener('focusout', start);
  }
  document.addEventListener('visibilitychange', function () { if (document.hidden) stop(); else start(); });
  load(0); load(1);
  start();
})();

/* ---------- karta placówek: wybór miasta → dane (bez preselekcji) ---------- */
(function () {
  var FAC = {
    szczecinek: { city: 'Szczecinek', address: 'ul. Kościuszki 57, 78-400 Szczecinek', tel: '+48943721451', phoneHtml: '<a href="tel:+48943721451">94 372 14 51</a> · <a href="tel:+48606965904">606 965 904</a>', email: 'szczecinek@rehamedica.info.pl' },
    szczecin:   { city: 'Szczecin',   address: 'ul. Wyszyńskiego 32-34, 78-411 Szczecin', tel: '+48530661982', phoneHtml: '<a href="tel:+48530661982">530 661 982</a>', email: 'szczecin@rehamedica.info.pl' },
    walcz:      { city: 'Wałcz',      address: 'ul. Gen. Andersa 9, 78-600 Wałcz', tel: '+48530661982', phoneHtml: '<a href="tel:+48530661982">530 661 982</a>', email: 'walcz@rehamedica.info.pl' },
    bialogard:  { city: 'Białogard',  address: 'ul. Najświętszej Marii Panny 19, 78-200 Białogard', tel: '+48535960018', phoneHtml: '<a href="tel:+48535960018">535 960 018</a>', email: 'bialogard@rehamedica.info.pl' },
    bobolice:   { city: 'Bobolice',   address: 'ul. Reymonta 2, 76-020 Bobolice', tel: '+48530645059', phoneHtml: '<a href="tel:+48530645059">530 645 059</a>', email: 'koszalin@rehamedica.info.pl' }
  };
  var card = document.getElementById('placowki');
  if (!card) return;

  // iOS/iPadOS → Apple Maps (natywnie, bez przechodzenia przez przeglądarkę),
  // reszta (Android, Windows, macOS desktop) → Google Maps.
  // Celowo tryb TRASY (dir/daddr), nie wyszukiwania — po kliknięciu od razu można jechać.
  var APPLE = /iPad|iPhone|iPod/.test(navigator.userAgent) ||
              (navigator.platform === 'MacIntel' && navigator.maxTouchPoints > 1);
  function linkDoMap(adres) {
    var q = encodeURIComponent(adres);
    return APPLE
      ? 'https://maps.apple.com/?daddr=' + q + '&dirflg=d'
      : 'https://www.google.com/maps/dir/?api=1&destination=' + q;
  }
  var tabs = Array.prototype.slice.call(card.querySelectorAll('.hcard__tabs [role="tab"]'));
  var empty = card.querySelector('[data-fac-empty]');
  var detail = card.querySelector('[data-fac-detail]');
  if (!tabs.length || !detail) return;
  tabs.forEach(function (t, i) { t.tabIndex = i === 0 ? 0 : -1; });

  function select(tab) {
    var d = FAC[tab.dataset.fac];
    if (!d) return;
    tabs.forEach(function (t) { t.setAttribute('aria-selected', String(t === tab)); });
    detail.querySelector('[data-city]').textContent = 'Reha Medica ' + d.city;

    // Adres + ikona nawigacji: ikona ma stać TUŻ przy nazwie miasta (nie przy krawędzi wiersza),
    // a jednocześnie nigdy nie zostać sama w nowym wierszu. Dlatego ostatnie słowo adresu
    // i ikonę sklejamy w jeden nierozdzielny blok (.hcard__glue = white-space:nowrap).
    // Budujemy węzłami DOM, nie innerHTML — zero ryzyka przy wstrzykiwaniu tekstu.
    var addrEl = detail.querySelector('[data-address]');
    var nav = detail.querySelector('.hcard__navico');   // referencję bierzemy PRZED czyszczeniem
    if (addrEl) {
      addrEl.textContent = '';
      var slowa = d.address.split(' ');
      var ostatnie = slowa.pop();
      if (slowa.length) addrEl.appendChild(document.createTextNode(slowa.join(' ') + ' '));
      var glue = document.createElement('span');
      glue.className = 'hcard__glue';
      glue.appendChild(document.createTextNode(ostatnie));
      if (nav) glue.appendChild(nav);
      addrEl.appendChild(glue);
    }

    var mapa = detail.querySelector('[data-maps]');
    if (mapa) {
      mapa.setAttribute('href', linkDoMap(d.address));
      mapa.setAttribute('aria-label', 'Nawiguj do placówki ' + d.city + ': ' + d.address +
        ' — otwiera ' + (APPLE ? 'Mapy Apple' : 'Mapy Google') + ' w nowej karcie');
    }
    detail.querySelector('[data-phone]').innerHTML = d.phoneHtml;
    var em = detail.querySelector('[data-email]');
    // hidden w HTML: dopóki nie wybrano placówki, link nie ma treści ani celu,
    // a pusty <a> to link bez nazwy dostępnej. Odsłaniamy dopiero z danymi.
    if (em) { em.textContent = d.email; em.setAttribute('href', 'mailto:' + d.email); em.hidden = false; }
    // Szukamy w obrębie całego hero, nie tylko w #placowki: CTA bywa poza kartą
    // (zależnie od układu). Wcześniej card.querySelector zwracał null i setAttribute
    // rzucał wyjątkiem — select() przerywał się przed odsłonięciem danych, więc karta
    // pokazywała stan pusty mimo zaznaczonego miasta. Stąd zakres + zabezpieczenia.
    var scope = card.closest('.hero') || document;
    var book = scope.querySelector('[data-book]');
    if (book) {
      book.setAttribute('href', 'tel:' + d.tel);
      book.setAttribute('aria-label', 'Umów wizytę — zadzwoń do placówki ' + d.city);
    }
    var see = scope.querySelector('[data-see]');
    if (see) {
      var slugMiasta = d.city.toLowerCase()
        .replace(/ą/g,'a').replace(/ć/g,'c').replace(/ę/g,'e').replace(/ł/g,'l')
        .replace(/ń/g,'n').replace(/ó/g,'o').replace(/ś/g,'s').replace(/[źż]/g,'z');
      // prefiks ścieżki: pusty na stronie głównej, „../../" na stronie placówki (data-link-prefix)
      var linkPrefix = card.getAttribute('data-link-prefix') || '';
      // Każda placówka ma widoczny link „Przejdź do placówki".
      // Szczecinek = centrala: /placowki/szczecinek/ jest przekierowaniem 301 na stronę główną.
      see.style.display = '';
      see.setAttribute('href', linkPrefix + 'placowki/' + slugMiasta + '/');
      see.setAttribute('aria-label', 'Przejdź do placówki ' + d.city);
    }
    if (empty) empty.hidden = true;
    detail.hidden = false;
  }
  function focusTab(i) {
    var n = (i + tabs.length) % tabs.length;
    tabs.forEach(function (t, j) { t.tabIndex = j === n ? 0 : -1; });
    tabs[n].focus();
  }
  tabs.forEach(function (tab, i) {
    tab.addEventListener('click', function () { select(tab); });
    tab.addEventListener('keydown', function (e) {
      switch (e.key) {
        case 'ArrowRight': case 'ArrowDown': e.preventDefault(); focusTab(i + 1); break;
        case 'ArrowLeft': case 'ArrowUp': e.preventDefault(); focusTab(i - 1); break;
        case 'Home': e.preventDefault(); focusTab(0); break;
        case 'End': e.preventDefault(); focusTab(tabs.length - 1); break;
      }
    });
  });

  // domyślnie zaznaczona placówka: na stronie placówki jej własna (data-fac-default),
  // na stronie głównej Szczecinek (centrala) — panel od razu pokazuje realny adres i kontakt
  var domyslna = card.getAttribute('data-fac-default') || 'szczecinek';
  var initial = tabs.filter(function (t) { return t.dataset.fac === domyslna; })[0] || tabs[0];
  if (initial) {
    select(initial);
    tabs.forEach(function (t) { t.tabIndex = t === initial ? 0 : -1; });
  }
})();

/* ---------- reveal sekcji przy scrollu (IntersectionObserver) ---------- */
(function () {
  var els = Array.prototype.slice.call(document.querySelectorAll('[data-animate]'));
  if (!els.length) return;
  if (REDUCE || !('IntersectionObserver' in window)) {
    els.forEach(function (el) { el.classList.add('is-visible'); });
    return;
  }
  var obs = new IntersectionObserver(function (entries) {
    entries.forEach(function (entry) {
      if (entry.isIntersecting) {
        entry.target.classList.add('is-visible');
        obs.unobserve(entry.target);
      }
    });
  }, { threshold: 0.1, rootMargin: '0px 0px -50px 0px' });
  els.forEach(function (el) { obs.observe(el); });
})();

/* ---------- wyszukiwarka po całym serwisie (lupa → pasek → wyniki z linkami) ----------
   Indeks wszystkich stron: window.RM_SEARCH (zdefiniowany na górze pliku). Działa na KAŻDEJ
   podstronie, bo main.js jest wszędzie. Adresy wyników liczone względnie (BASE z src main.js),
   więc działają i na produkcji (rehamedica.info.pl), i na podglądzie github.io (/rehamedica/). */
(function () {
  var header = document.getElementById('site-header');
  var openBtn = document.querySelector('[data-search-open]');
  var closeBtn = document.querySelector('[data-search-close]');
  var input = document.getElementById('svc-search');
  if (!header || !openBtn || !input) return;

  var DATA = window.RM_SEARCH || [];
  var self = document.querySelector('script[src*="js/main.js"]');
  var BASE = self ? (self.getAttribute('src') || '').replace(/js\/main\.js.*$/, '') : '';

  function norm(s) { return (s || '').toLowerCase()
    .replace(/ą/g,'a').replace(/ć/g,'c').replace(/ę/g,'e').replace(/ł/g,'l').replace(/ń/g,'n').replace(/ó/g,'o').replace(/ś/g,'s').replace(/ż/g,'z').replace(/ź/g,'z'); }
  function esc(s) { return (s || '').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;'); }

  // wstępna normalizacja pól: _t = tytuł (do rankingu), _h = tytuł+opis+kategoria (do dopasowania)
  DATA.forEach(function (d) { d._t = norm(d.t); d._h = norm((d.k || '') + ' ' + (d.c || '')); });

  // panel wyników wstrzykiwany do paska
  var box = document.createElement('div');
  box.className = 'nav-search__results';
  box.id = 'svc-search-results';
  box.setAttribute('role', 'listbox');
  box.hidden = true;
  input.insertAdjacentElement('afterend', box);
  input.setAttribute('role', 'combobox');
  input.setAttribute('aria-autocomplete', 'list');
  input.setAttribute('aria-controls', box.id);
  input.setAttribute('aria-expanded', 'false');

  var results = [], active = -1;

  function search() {
    var raw = input.value.trim();
    var q = norm(raw);
    if (!q) { results = []; active = -1; render(''); return; }
    var toks = q.split(/\s+/).filter(Boolean);
    results = DATA
      .map(function (d) {
        var ok = toks.every(function (t) { return d._t.indexOf(t) !== -1 || d._h.indexOf(t) !== -1; });
        if (!ok) return null;
        var s = 0;
        if (d._t === q) s += 1000;
        else if (d._t.indexOf(q) === 0) s += 300;
        else if (d._t.indexOf(q) !== -1) s += 150;
        toks.forEach(function (t) { if (d._t.indexOf(t) !== -1) s += 40; if (d._h.indexOf(t) !== -1) s += 10; });
        return { d: d, s: s };
      })
      .filter(Boolean)
      .sort(function (a, b) { return b.s - a.s || a.d.t.localeCompare(b.d.t, 'pl'); })
      .slice(0, 8)
      .map(function (x) { return x.d; });
    active = -1;
    render(raw);
  }

  function render(raw) {
    if (!raw) { box.hidden = true; box.innerHTML = ''; input.setAttribute('aria-expanded', 'false'); input.removeAttribute('aria-activedescendant'); return; }
    if (!results.length) {
      box.innerHTML = '<p class="nav-search__empty">Brak wyników dla „' + esc(raw) + '". Spróbuj: masaż, kręgosłup, NFZ, Wałcz…</p>';
    } else {
      box.innerHTML = results.map(function (d, i) {
        return '<a class="nav-search__hit" role="option" id="ssr-' + i + '" href="' + esc(BASE + d.u) + '">'
          + '<span class="nav-search__hit-t">' + esc(d.t) + '</span>'
          + (d.c ? '<span class="nav-search__hit-c">' + esc(d.c) + '</span>' : '')
          + '</a>';
      }).join('');
    }
    box.hidden = false;
    input.setAttribute('aria-expanded', 'true');
  }

  function hits() { return box.querySelectorAll('.nav-search__hit'); }
  function highlight(n) {
    var h = hits(); if (!h.length) return;
    n = (n + h.length) % h.length;
    for (var i = 0; i < h.length; i++) { h[i].classList.toggle('is-active', i === n); h[i].setAttribute('aria-selected', i === n ? 'true' : 'false'); }
    active = n;
    input.setAttribute('aria-activedescendant', h[n].id);
    h[n].scrollIntoView({ block: 'nearest' });
  }
  function go(a) { var h = hits(); var el = (a >= 0 && h[a]) ? h[a] : h[0]; if (el) window.location.href = el.getAttribute('href'); }

  function open() {
    header.classList.add('search-open');
    openBtn.setAttribute('aria-expanded', 'true');
    setTimeout(function () { input.focus(); }, 60);
    document.addEventListener('keydown', onKey);
  }
  function close(returnFocus) {
    header.classList.remove('search-open');
    openBtn.setAttribute('aria-expanded', 'false');
    input.value = ''; input.blur();
    results = []; active = -1; render('');
    document.removeEventListener('keydown', onKey);
    if (returnFocus !== false) openBtn.focus();
  }
  function onKey(e) { if (e.key === 'Escape') { e.preventDefault(); close(); } }

  openBtn.addEventListener('click', open);
  if (closeBtn) closeBtn.addEventListener('click', function () { close(false); });
  input.addEventListener('input', search);
  input.addEventListener('keydown', function (e) {
    if (e.key === 'ArrowDown') { e.preventDefault(); highlight(active + 1); }
    else if (e.key === 'ArrowUp') { e.preventDefault(); highlight(active - 1); }
    else if (e.key === 'Enter') { if (results.length) { e.preventDefault(); go(active); } }
  });
  // klik poza paskiem chowa listę wyników (całą wyszukiwarkę zamyka X lub Esc)
  document.addEventListener('click', function (e) {
    if (!header.classList.contains('search-open')) return;
    if (e.target.closest('.nav-search') || e.target.closest('[data-search-open]')) return;
    box.hidden = true; input.setAttribute('aria-expanded', 'false');
  });
})();

/* ---------- selektory sekcji: Usługi (explorer) + Konsultacje (rozdział) ----------
   Sterowane WYŁĄCZNIE klikiem. Auto-rotacja zdjęta: jedynym ruchem na stronie zostaje hero,
   a panel nie podmienia treści pod czytającym. */
(function () {
  function rotator(o) {
    var items = Array.prototype.slice.call(document.querySelectorAll(o.item));
    var titleEl = document.querySelector(o.title);
    var descEl = document.querySelector(o.desc);
    var countEl = document.querySelector(o.count);
    var ctaEl = document.querySelector(o.cta);
    var imgs = o.imgs ? Array.prototype.slice.call(document.querySelectorAll(o.imgs)) : null;
    var host = document.querySelector(o.swapHost);
    var section = document.querySelector(o.section);
    var total = o.data.length, cur = 0;
    if (!titleEl || !total) return;

    function mark(idx) { items.forEach(function (it, i) { it.classList.toggle('is-current', i === idx); }); }
    function loadImg(n) {
      var el = imgs && imgs[n];
      if (el && el.dataset.bg) { el.style.backgroundImage = "url('" + el.dataset.bg + "')"; delete el.dataset.bg; }
    }
    function show(idx) {
      if (idx === cur) return;
      loadImg(idx);
      if (imgs) { if (imgs[cur]) imgs[cur].classList.remove('is-active'); if (imgs[idx]) imgs[idx].classList.add('is-active'); }
      mark(idx);
      if (host) host.classList.add('is-swapping');
      setTimeout(function () {
        var d = o.data[idx];
        titleEl.textContent = d.t;
        if (descEl) descEl.textContent = d.d;
        if (countEl) countEl.textContent = (idx + 1 < 10 ? '0' : '') + (idx + 1);
        // Usługa bez własnej strony (np. konsultacje socjologiczne — czekają na serwis
        // psychologiczny) nie dostaje ślepego przycisku: chowamy go zamiast prowadzić donikąd.
        if (ctaEl) {
          var cel = d.h || '#';
          ctaEl.setAttribute('href', cel);
          ctaEl.hidden = (cel === '#');
        }
        if (host) host.classList.remove('is-swapping');
      }, REDUCE ? 0 : 280);
      cur = idx;
    }
    /* Pozycje indeksu są prawdziwymi linkami do stron usług (crawlowalne, działa
       „otwórz w nowej karcie"), ale nie tracimy przez to panelu:
         • klik w pozycję NIEwybraną  → przełącza panel (blokujemy nawigację),
         • klik w pozycję już wybraną → przechodzi na jej stronę.
       Dzięki temu pacjent najpierw przegląda opisy, a wchodzi dopiero świadomie. */
    var stageEl = section && (section.querySelector(o.pause) || section);
    items.forEach(function (it, i) {
      it.addEventListener('click', function (e) {
        var adres = it.getAttribute('href') || '#';
        var juzWybrana = it.classList.contains('is-current');
        if (adres !== '#' && juzWybrana) return;   // druga próba = wejście na stronę
        e.preventDefault();
        show(i);
        // na mobile panel jest nad indeksem — przewiń, żeby zmiana była widoczna
        if (stageEl && window.matchMedia('(max-width: 768px)').matches) {
          stageEl.scrollIntoView({ behavior: REDUCE ? 'auto' : 'smooth', block: 'center' });
        }
      });
    });

    mark(0);
    loadImg(0);
  }

  rotator({
    item: '.svc__item', title: '[data-svc-title]', desc: '[data-svc-desc]', count: '[data-svc-counter]',
    cta: '[data-svc-cta]', imgs: '.svc__img', swapHost: '.svc__panel', section: '.svc', pause: '.svc__stage',
    data: [
      { t: 'Rehabilitacja i fizjoterapia', d: 'Poradnia rehabilitacyjna, ośrodek rehabilitacji ogólnoustrojowej oraz indywidualna terapia i zabiegi fizjoterapeutyczne prowadzone przez zespół Reha Medica.', h: 'uslugi/rehabilitacja/rehabilitacja-lecznicza/' },
      { t: 'Konsultacje medyczne', d: 'Konsultacje ortopedyczne, neurologiczne i fizjoterapeutyczne w jednym miejscu.', h: 'uslugi/konsultacje/' },
      { t: 'Logopedia i neurologopedia', d: 'Diagnoza i terapia zaburzeń mowy.', h: 'uslugi/poradnia-logopedyczna/' },
      { t: 'Rehabilitacja dzieci i młodzieży', d: 'Terapia dopasowana do młodszych pacjentów.', h: 'uslugi/rehabilitacja/rehabilitacja-dzieci-i-mlodziezy/' },
      { t: 'Rehabilitacja domowa', d: 'Terapia prowadzona w domu pacjenta.', h: 'uslugi/rehabilitacja/rehabilitacja-domowa/' },
      { t: 'Odnowa biologiczna i masaże', d: 'Sauna, kriokomora oraz masaże lecznicze i relaksacyjne.', h: 'uslugi/odnowa-biologiczna/' }
    ]
  });

  rotator({
    item: '.kons__row', title: '[data-kons-title]', desc: '[data-kons-desc]', count: '[data-kons-counter]',
    cta: '[data-kons-cta]', imgs: null, swapHost: '.kons__active', section: '.kons', pause: '.kons__active',
    data: [
      { t: 'Konsultacje fizjoterapeutyczne', d: 'Konsultacja z fizjoterapeutą i ustalenie dalszego postępowania terapeutycznego.', h: 'uslugi/konsultacje/fizjoterapeutyczna/' },
      { t: 'Konsultacje ortopedyczne', d: 'Konsultacja w zakresie problemów narządu ruchu.', h: 'uslugi/konsultacje/ortopedyczna/' },
      { t: 'Konsultacje logopedyczne i neurologopedyczne', d: 'Konsultacja dotycząca diagnostyki i terapii zaburzeń mowy.', h: 'uslugi/konsultacje/logopedyczna-neurologopedyczna/' },
      { t: 'Konsultacje neurologiczne', d: 'Konsultacja w zakresie schorzeń układu nerwowego.', h: 'uslugi/konsultacje/neurologiczna/' },
      { t: 'Konsultacje socjologiczne', d: 'Konsultacja i wsparcie w obszarze sytuacji społecznej pacjenta.', h: 'uslugi/konsultacje/socjologiczna/' }
    ]
  });
})();

/* ---------- newsletter (UI; backend DO PODŁĄCZENIA) ---------- */
(function () {
  var form = document.getElementById('newsletter-form');
  if (!form) return;
  var note = form.querySelector('[data-nl-note]');
  var tel = document.getElementById('nl-tel');
  var smsBox = form.querySelector('[data-sms-consent]');
  var smsInput = smsBox && smsBox.querySelector('input');

  // Telefon jest opcjonalny, ale SMS to osobny kanał marketingowy: zgoda na SMS
  // pojawia się i staje wymagana DOPIERO gdy ktoś poda numer. Kto nie chce SMS-ów,
  // nie widzi dodatkowego pola do odklikania.
  function przelaczZgodeSms() {
    if (!smsBox || !smsInput) return;
    var podanyNumer = tel && tel.value.trim() !== '';
    smsBox.hidden = !podanyNumer;
    smsInput.required = podanyNumer;
    if (!podanyNumer) smsInput.checked = false;
  }
  if (tel) tel.addEventListener('input', przelaczZgodeSms);
  przelaczZgodeSms();

  form.addEventListener('submit', function (e) {
    e.preventDefault();
    if (!form.checkValidity()) { form.reportValidity(); return; }
    if (note) {
      note.hidden = false;
      note.textContent = tel && tel.value.trim()
        ? 'Formularz zostanie uruchomiony po podłączeniu systemu wysyłki e-mail i SMS.'
        : 'Formularz newslettera zostanie uruchomiony po podłączeniu systemu wysyłki.';
    }
  });
})();

/* ---------- Opinie pacjentów: karuzela opinii Google (dane z data/reviews.json) ---------- */
(function () {
  var root = document.querySelector('.reviews');
  if (!root) return;
  var track = root.querySelector('[data-rv-track]');
  var prev = root.querySelector('[data-rv-prev]');
  var next = root.querySelector('[data-rv-next]');
  // paleta awatarów — ciepłe brązy przyciemnione tak, by inicjały (biały tekst) miały >= 4,5:1
  var AV = ['#6b5842', '#5d5142', '#7a6349', '#655445', '#71604b', '#584a3c'];

  function esc(s){ return (s == null ? '' : String(s)).replace(/[&<>"]/g, function (c){ return { '&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;' }[c]; }); }
  function setText(el, t){ if (el) el.textContent = t; }
  function stars(n){ n = Math.max(0, Math.min(5, n|0)); return '★★★★★'.slice(0, n) + '☆☆☆☆☆'.slice(0, 5 - n); }

  function render(data){
    setText(root.querySelector('[data-rv-rating]'), String(data.rating).replace('.', ','));
    setText(root.querySelector('[data-rv-count]'), String(data.reviewCount));
    if (data.ratingLabel) setText(root.querySelector('[data-rv-label]'), data.ratingLabel);
    var sEl = root.querySelector('[data-rv-stars]'); if (sEl) sEl.textContent = '★★★★★';
    var moreLink = root.querySelector('[data-rv-more]');
    if (moreLink && data.googleMapsUrl) moreLink.setAttribute('href', data.googleMapsUrl);

    track.innerHTML = '';
    (data.reviews || []).forEach(function (r, i){
      var rating = r.rating || 5;
      var li = document.createElement('li');
      li.className = 'rv-card';
      li.setAttribute('role', 'listitem');
      li.innerHTML =
        '<div class="rv-card__head">' +
          '<span class="rv-card__avatar" style="--rv-av:' + AV[i % AV.length] + '" aria-hidden="true">' + esc(r.initials || '') + '</span>' +
          '<div class="rv-card__meta">' +
            '<p class="rv-card__author">' + esc(r.author) + '</p>' +
            '<p class="rv-card__date">' + esc(r.dateText || '') + '</p>' +
          '</div>' +
          '<img class="rv-card__src" src="assets/img/google-g-mono.svg" alt="Opinia z Google" width="18" height="18">' +
        '</div>' +
        '<div class="rv-card__stars" aria-hidden="true">' + stars(rating) + '</div>' +
        '<span class="visually-hidden">Ocena ' + rating + ' na 5.</span>' +
        '<p class="rv-card__text">' + esc(r.text) + '</p>' +
        '<button class="rv-card__more" type="button" aria-expanded="false" hidden>Czytaj więcej</button>';
      track.appendChild(li);
    });

    function wireReadMore(){
      track.querySelectorAll('.rv-card').forEach(function (card){
        var txt = card.querySelector('.rv-card__text');
        var btn = card.querySelector('.rv-card__more');
        if (!txt || !btn) return;
        btn.hidden = !(txt.scrollHeight - txt.clientHeight > 4);
      });
    }
    track.addEventListener('click', function (e){
      var btn = e.target.closest('.rv-card__more'); if (!btn) return;
      var card = btn.closest('.rv-card');
      var exp = card.classList.toggle('is-expanded');
      btn.setAttribute('aria-expanded', String(exp));
      btn.textContent = exp ? 'Zwiń' : 'Czytaj więcej';
      update();
    });
    wireReadMore();
    window.addEventListener('load', wireReadMore);

    setupCarousel();
  }

  var raf = 0;
  function maxScroll(){ return track.scrollWidth - track.clientWidth; }
  function update(){
    var max = maxScroll(), x = track.scrollLeft;
    if (prev) prev.disabled = x <= 2;
    if (next) next.disabled = x >= max - 2;
  }
  function step(){
    var card = track.querySelector('.rv-card');
    if (card){ var gap = parseFloat(getComputedStyle(track).columnGap) || 24; return card.getBoundingClientRect().width + gap; }
    return track.clientWidth * 0.9;
  }
  function go(dir){ track.scrollBy({ left: dir * step(), behavior: REDUCE ? 'auto' : 'smooth' }); }

  function setupCarousel(){
    if (prev) prev.addEventListener('click', function (){ go(-1); });
    if (next) next.addEventListener('click', function (){ go(1); });
    track.addEventListener('scroll', function (){ if (!raf) raf = requestAnimationFrame(function (){ update(); raf = 0; }); }, { passive: true });
    track.addEventListener('keydown', function (e){
      if (e.key === 'ArrowRight'){ e.preventDefault(); go(1); }
      else if (e.key === 'ArrowLeft'){ e.preventDefault(); go(-1); }
      else if (e.key === 'Home'){ e.preventDefault(); track.scrollTo({ left: 0, behavior: REDUCE ? 'auto' : 'smooth' }); }
      else if (e.key === 'End'){ e.preventDefault(); track.scrollTo({ left: maxScroll(), behavior: REDUCE ? 'auto' : 'smooth' }); }
    });
    // przeciąganie myszą
    var down = false, sx = 0, sl = 0, moved = false;
    track.addEventListener('pointerdown', function (e){
      if (e.pointerType !== 'mouse') return;
      down = true; sx = e.clientX; sl = track.scrollLeft; moved = false; track.classList.add('is-dragging');
    });
    window.addEventListener('pointermove', function (e){
      if (!down) return; var dx = e.clientX - sx; if (Math.abs(dx) > 3) moved = true; track.scrollLeft = sl - dx;
    });
    window.addEventListener('pointerup', function (){ if (down){ down = false; track.classList.remove('is-dragging'); } });
    track.addEventListener('click', function (e){ if (moved){ e.preventDefault(); e.stopPropagation(); moved = false; } }, true);
    window.addEventListener('resize', function (){ if (!raf) raf = requestAnimationFrame(function (){ update(); raf = 0; }); });
    update();
  }

  fetch('data/reviews.json').then(function (r){ return r.json(); }).then(render).catch(function (err){
    var c = root.querySelector('.rv-slider');
    if (c) c.innerHTML = '<p style="color:var(--color-text-2)">Nie udało się wczytać opinii.</p>';
    if (window.console) console.warn('Nie udało się wczytać data/reviews.json', err);
  });
})();

/* ---------- panel wyboru placówki (otwierany przez [data-booking]) ----------
   Akordeon zamiast dwóch ekranów: pacjent widzi wszystkie placówki naraz i jednym
   tapnięciem dostaje numer. Żadna placówka NIE jest preselektowana (reguła CLAUDE.md).
   Dane identyczne z kartą placówek w hero — jedno źródło prawdy. */
(function () {
  var bk = document.getElementById('bk');
  var lista = bk && bk.querySelector('[data-bk-list]');
  var wyzwalacze = document.querySelectorAll('[data-booking]');
  if (!bk || !lista || !wyzwalacze.length) return;

  var PLACOWKI = [
    { id:'szczecinek', miasto:'Szczecinek', adres:'ul. Kościuszki 57, 78-400 Szczecinek',
      tel:[{n:'94 372 14 51', t:'+48943721451'},{n:'606 965 904', t:'+48606965904'}],
      mail:'szczecinek@rehamedica.info.pl' },
    { id:'szczecin', miasto:'Szczecin', adres:'ul. Wyszyńskiego 32-34, 78-411 Szczecin',
      tel:[{n:'530 661 982', t:'+48530661982'}], mail:'szczecin@rehamedica.info.pl' },
    { id:'walcz', miasto:'Wałcz', adres:'ul. Gen. Andersa 9, 78-600 Wałcz',
      tel:[{n:'530 661 982', t:'+48530661982'}], mail:'walcz@rehamedica.info.pl' },
    { id:'bialogard', miasto:'Białogard', adres:'ul. Najświętszej Marii Panny 19, 78-200 Białogard',
      tel:[{n:'535 960 018', t:'+48535960018'}], mail:'bialogard@rehamedica.info.pl' },
    { id:'bobolice', miasto:'Bobolice', adres:'ul. Reymonta 2, 76-020 Bobolice',
      tel:[{n:'530 645 059', t:'+48530645059'}], mail:'koszalin@rehamedica.info.pl' }
  ];

  // budujemy węzłami DOM, nie innerHTML — zero ryzyka przy wstawianiu tekstu
  function el(tag, klasa, tekst) {
    var e = document.createElement(tag);
    if (klasa) e.className = klasa;
    if (tekst != null) e.textContent = tekst;
    return e;
  }
  function ikona(id) {
    var svg = document.createElementNS('http://www.w3.org/2000/svg', 'svg');
    svg.setAttribute('class', 'ico'); svg.setAttribute('aria-hidden', 'true'); svg.setAttribute('focusable', 'false');
    var use = document.createElementNS('http://www.w3.org/2000/svg', 'use');
    use.setAttribute('href', '#' + id); svg.appendChild(use); return svg;
  }

  /* Które placówki świadczą TĘ usługę — z atrybutu strony (macierz usługa-placówka).
     Nigdy nie zakładamy, że każda placówka ma każdą usługę (reguła CLAUDE.md).
     Brak atrybutu = panel ogólnokontaktowy, wtedy wszystkie placówki. */
  var deklaracja = (bk.getAttribute('data-bk-places') || '').trim();
  var DOSTEPNE = deklaracja
    ? deklaracja.split(/\s+/).map(function (id) {
        var t = PLACOWKI.filter(function (p) { return p.id === id; })[0];
        if (!t) console.warn('[bk] nieznane id placówki w data-bk-places: ' + id);
        return t;
      }).filter(Boolean)
    : PLACOWKI.slice();
  if (!DOSTEPNE.length) return; // brak potwierdzonej placówki = nie pokazujemy zmyślonego wyboru

  var usluga = (bk.getAttribute('data-bk-service') || '').trim();
  var tytul = bk.querySelector('#bk-title');
  var podtytul = bk.querySelector('[data-bk-sub]');

  // adres + numery + mail — wspólne dla obu wariantów
  function szczegoly(p) {
    var panel = el('div', 'bk__detail');
    panel.id = 'bk-p-' + p.id;

    var adres = el('p', 'bk__addr');
    adres.appendChild(ikona('i-pin'));
    adres.appendChild(el('span', null, p.adres));
    panel.appendChild(adres);

    // numer jako duży przycisk — główna akcja: zadzwoń i umów
    p.tel.forEach(function (t) {
      var a = el('a', 'bk__call');
      a.href = 'tel:' + t.t;
      a.setAttribute('aria-label', 'Zadzwoń do placówki ' + p.miasto + ': ' + t.n);
      a.appendChild(ikona('i-phone'));
      a.appendChild(el('span', null, t.n));
      panel.appendChild(a);
    });

    var mail = el('a', 'bk__mail');
    mail.href = 'mailto:' + p.mail;
    mail.appendChild(ikona('i-mail'));
    mail.appendChild(el('span', null, p.mail));
    panel.appendChild(mail);

    return panel;
  }

  if (DOSTEPNE.length === 1) {
    /* Jedna placówka = nie ma czego wybierać. Zamiast pozorowanego wyboru
       (lista z jedną pozycją) pokazujemy numer od razu — o jedno tapnięcie mniej.
       To nie jest preselekcja: alternatywy dla tej usługi po prostu nie istnieją. */
    var jedyna = DOSTEPNE[0];
    if (tytul) tytul.textContent = 'Umów wizytę';
    // Bez podtytułu: nazwa miasta jest już nagłówkiem pozycji poniżej, a dopisek
    // „usługa — placówka X” dawał masło maślane w rodzaju
    // „Wizyta w placówce Białogard — placówka Białogard”.
    var liSolo = el('li', 'bk__item bk__item--solo');
    liSolo.appendChild(el('p', 'bk__solo', jedyna.miasto));
    liSolo.appendChild(szczegoly(jedyna));
    lista.appendChild(liSolo);
  } else {
    if (tytul) tytul.textContent = 'Umów wizytę';
    DOSTEPNE.forEach(function (p) {
      var li = el('li', 'bk__item');

      var btn = el('button', 'bk__row');
      btn.type = 'button';
      btn.setAttribute('aria-expanded', 'false');
      btn.setAttribute('aria-controls', 'bk-p-' + p.id);
      btn.appendChild(el('span', 'bk__city', p.miasto));
      btn.appendChild(ikona('i-chevron')).classList.add('bk__chev');
      li.appendChild(btn);

      var panel = szczegoly(p);
      panel.hidden = true;
      li.appendChild(panel);
      lista.appendChild(li);

      btn.addEventListener('click', function () {
        var otwarty = btn.getAttribute('aria-expanded') === 'true';
        // tylko jedna placówka rozwinięta naraz — mniej scrollowania
        lista.querySelectorAll('.bk__row[aria-expanded="true"]').forEach(function (b) {
          b.setAttribute('aria-expanded', 'false');
          document.getElementById(b.getAttribute('aria-controls')).hidden = true;
        });
        if (!otwarty) { btn.setAttribute('aria-expanded', 'true'); panel.hidden = false; }
      });
    });
  }

  var ostatni = null;
  function fokusowalne() {
    return Array.prototype.slice.call(bk.querySelectorAll('a[href],button:not([disabled])'))
      .filter(function (e) { return e.offsetWidth > 0 || e.offsetHeight > 0; });
  }
  function onKey(e) {
    if (e.key === 'Escape') { e.preventDefault(); zamknij(); return; }
    if (e.key === 'Tab') {
      var f = fokusowalne(); if (!f.length) return;
      var pierwszy = f[0], ostatniEl = f[f.length - 1];
      if (e.shiftKey && document.activeElement === pierwszy) { e.preventDefault(); ostatniEl.focus(); }
      else if (!e.shiftKey && document.activeElement === ostatniEl) { e.preventDefault(); pierwszy.focus(); }
    }
  }
  function otworz(trigger) {
    ostatni = trigger;
    bk.hidden = false;
    // dwie klatki: .is-open startuje przejście, a focus() działa dopiero gdy panel jest widoczny
    requestAnimationFrame(function () {
      bk.classList.add('is-open');
      requestAnimationFrame(function () {
        // przy jednej placówce nie ma wiersza wyboru — fokus idzie prosto na numer
        var f = bk.querySelector('.bk__row, .bk__call');
        if (f) f.focus();
      });
    });
    document.body.style.overflow = 'hidden';
    document.addEventListener('keydown', onKey);
  }
  function zamknij() {
    bk.classList.remove('is-open');
    document.body.style.overflow = '';
    document.removeEventListener('keydown', onKey);
    // zwiń rozwinięte, żeby panel otwierał się zawsze w tym samym stanie
    lista.querySelectorAll('.bk__row[aria-expanded="true"]').forEach(function (b) {
      b.setAttribute('aria-expanded', 'false');
      document.getElementById(b.getAttribute('aria-controls')).hidden = true;
    });
    setTimeout(function () { bk.hidden = true; }, 260);
    if (ostatni) ostatni.focus();
  }

  Array.prototype.forEach.call(wyzwalacze, function (w) {
    w.addEventListener('click', function (e) { e.preventDefault(); otworz(w); });
  });
  Array.prototype.forEach.call(bk.querySelectorAll('[data-bk-close]'), function (c) {
    c.addEventListener('click', zamknij);
  });
})();

/* ---------- adres → nawigacja (strony placówek) ----------
   Karta na stronie głównej buduje ten link sama, gdy pacjent wybierze placówkę.
   Tutaj obsługujemy przypadek, w którym adres jest w kodzie od razu: bierzemy tylko
   te elementy, które mają już wpisany adres, więc karta na home pozostaje nietknięta. */
(function () {
  var APPLE = /iPad|iPhone|iPod/.test(navigator.userAgent) ||
              (navigator.platform === 'MacIntel' && navigator.maxTouchPoints > 1);
  Array.prototype.forEach.call(document.querySelectorAll('[data-maps]'), function (a) {
    var el = a.querySelector('[data-address]');
    var adres = el && el.textContent.trim();
    if (!adres) return;                       // pusty = karta na stronie głównej
    var q = encodeURIComponent(adres);
    a.setAttribute('href', APPLE
      ? 'https://maps.apple.com/?daddr=' + q + '&dirflg=d'
      : 'https://www.google.com/maps/dir/?api=1&destination=' + q);
    a.setAttribute('aria-label', 'Nawiguj do: ' + adres);
  });
})();

/* ---------- ankieta satysfakcji (/ankieta-satysfakcji/) ----------
   Walidacja po stronie klienta + wysyłka. Backendu JESZCZE NIE MA:
   `data-endpoint` na formularzu jest pusty. Świadomie NIE pokazujemy
   podziękowania bez potwierdzonego zapisu — pacjent musiałby uwierzyć,
   że opinia dotarła, a nie dotarłaby nigdzie. Po podpięciu endpointu
   wystarczy wypełnić atrybut, kod niżej jest gotowy. */
(function () {
  var form = document.getElementById('ankieta');
  if (!form) return;

  var status = form.querySelector('[data-status]');
  var ts = form.querySelector('[data-ts]');
  if (ts) ts.value = String(Math.floor(Date.now() / 1000));

  function grupy() {
    var out = [], widziane = {};
    form.querySelectorAll('input[type="radio"][required]').forEach(function (r) {
      if (widziane[r.name]) return;
      widziane[r.name] = 1;
      out.push({ nazwa: r.name, pole: r.closest('.ank__q') });
    });
    return out;
  }

  function pokazBlad(g, jest) {
    if (!g.pole) return;
    var err = g.pole.querySelector('[data-err]');
    if (err) err.hidden = jest;
    if (jest) g.pole.removeAttribute('data-invalid');
    else g.pole.setAttribute('data-invalid', '');
  }

  // czyszczenie błędu od razu po zaznaczeniu — bez czekania na kolejny submit
  form.addEventListener('change', function (e) {
    if (e.target.type !== 'radio') return;
    var pole = e.target.closest('.ank__q');
    if (!pole) return;
    pole.removeAttribute('data-invalid');
    var err = pole.querySelector('[data-err]');
    if (err) err.hidden = true;
  });

  form.addEventListener('submit', function (e) {
    e.preventDefault();
    if (form.querySelector('[name="ps_hp"]').value !== '') return; // bot

    var brakujace = [];
    grupy().forEach(function (g) {
      var ok = !!form.querySelector('input[name="' + CSS.escape(g.nazwa) + '"]:checked');
      pokazBlad(g, ok);
      if (!ok) brakujace.push(g);
    });

    if (brakujace.length) {
      status.setAttribute('data-typ', 'blad');
      status.textContent = brakujace.length === 1
        ? 'Zostało jedno pytanie bez odpowiedzi.'
        : 'Bez odpowiedzi: ' + brakujace.length + ' pytań.';
      var p = brakujace[0].pole;
      p.scrollIntoView({ behavior: 'smooth', block: 'center' });
      var r = p.querySelector('input[type="radio"]');
      if (r) r.focus({ preventScroll: true });
      return;
    }

    var endpoint = (form.getAttribute('data-endpoint') || '').trim();
    if (!endpoint) {
      status.setAttribute('data-typ', 'blad');
      status.textContent = 'Ankieta jest kompletnie wypełniona, ale wysyłka nie jest jeszcze '
        + 'uruchomiona: trwają prace nad nową stroną. Prosimy spróbować ponownie po jej starcie.';
      return;
    }

    var btn = form.querySelector('button[type="submit"]');
    btn.disabled = true;
    status.removeAttribute('data-typ');
    status.textContent = 'Wysyłanie…';
    fetch(endpoint, { method: 'POST', body: new FormData(form) })
      .then(function (r) { if (!r.ok) throw new Error(r.status); return r; })
      .then(function () {
        form.innerHTML = '<p class="ank__status" role="status">Dziękujemy. Ankieta została wysłana '
          + 'anonimowo i pomoże nam poprawiać jakość opieki.</p>';
      })
      .catch(function () {
        btn.disabled = false;
        status.setAttribute('data-typ', 'blad');
        status.textContent = 'Nie udało się wysłać ankiety. Prosimy spróbować ponownie za chwilę.';
      });
  });
})();

/* ---------- menu dostępności (self-hosted; parytet z dawnym widgetem wcag.dock.codes) ----------
   Stan w localStorage['rm-a11y']; tryby stosowane atrybutami na <html>.
   Wczesne zastosowanie (bez mignięcia) robi skrypt inline w <head>. */
(function () {
  var fab = document.querySelector('.a11y-fab');
  var panel = document.getElementById('a11y-panel');
  if (!fab || !panel) return;

  var SKALA = [90, 100, 112, 125, 140];
  var TOGGLE = ['contrast', 'invert', 'gray', 'underline', 'cursor', 'font', 'hideimg', 'guide', 'reduce'];
  var DOMYSLNY = { scale: 100, space: 0, lineh: 0 };
  TOGGLE.forEach(function (k) { DOMYSLNY[k] = false; });

  var stan;
  try { stan = Object.assign({}, DOMYSLNY, JSON.parse(localStorage.getItem('rm-a11y') || '{}')); }
  catch (e) { stan = Object.assign({}, DOMYSLNY); }

  var h = document.documentElement;
  function zapisz() { try { localStorage.setItem('rm-a11y', JSON.stringify(stan)); } catch (e) {} }

  function zastosuj() {
    h.style.fontSize = stan.scale === 100 ? '' : stan.scale + '%';
    // powiększony tekst rozbija poziomy nagłówek (media queries nie widzą skali) —
    // wymuszamy wtedy układ hamburgera atrybutem data-a11y-compact
    if (stan.scale > 100 || stan.font) h.setAttribute('data-a11y-compact', ''); else h.removeAttribute('data-a11y-compact');
    if (stan.space) h.setAttribute('data-a11y-space', stan.space); else h.removeAttribute('data-a11y-space');
    if (stan.lineh) h.setAttribute('data-a11y-lineh', stan.lineh); else h.removeAttribute('data-a11y-lineh');
    TOGGLE.forEach(function (k) {
      if (stan[k]) h.setAttribute('data-a11y-' + k, ''); else h.removeAttribute('data-a11y-' + k);
      var b = panel.querySelector('[data-a11y-' + k + ']');
      if (b) b.setAttribute('aria-pressed', stan[k] ? 'true' : 'false');
    });
    // wartości stepperów w UI
    ustawVal('scale', stan.scale + '%');
    ustawVal('space', ETY_POZ[stan.space]);
    ustawVal('lineh', ETY_POZ[stan.lineh]);
    // granice przycisków
    granica('scale', SKALA.indexOf(stan.scale), SKALA.length);
    granica('space', stan.space, 3);
    granica('lineh', stan.lineh, 3);
    guide.hookup();
  }
  var ETY_POZ = ['wył.', 'średnie', 'duże'];
  function ustawVal(klucz, txt) { var e = panel.querySelector('[data-val="' + klucz + '"]'); if (e) e.textContent = txt; }
  function granica(klucz, idx, max) {
    var dec = panel.querySelector('[data-dec="' + klucz + '"]'), inc = panel.querySelector('[data-inc="' + klucz + '"]');
    if (dec) dec.disabled = idx <= 0;
    if (inc) inc.disabled = idx >= max - 1;
  }

  // steppery
  panel.querySelectorAll('[data-dec],[data-inc]').forEach(function (btn) {
    var dir = btn.hasAttribute('data-inc') ? 1 : -1;
    var klucz = btn.getAttribute('data-inc') || btn.getAttribute('data-dec');
    btn.addEventListener('click', function () {
      if (klucz === 'scale') {
        var i = SKALA.indexOf(stan.scale); if (i < 0) i = 1;
        i = Math.max(0, Math.min(SKALA.length - 1, i + dir));
        stan.scale = SKALA[i];
      } else {
        stan[klucz] = Math.max(0, Math.min(2, (stan[klucz] || 0) + dir));
      }
      zastosuj(); zapisz();
    });
  });

  // przełączniki
  TOGGLE.forEach(function (k) {
    var b = panel.querySelector('[data-a11y-' + k + ']');
    if (b) b.addEventListener('click', function () { stan[k] = !stan[k]; zastosuj(); zapisz(); });
  });

  // reset
  var reset = panel.querySelector('[data-a11y-reset]');
  if (reset) reset.addEventListener('click', function () { stan = Object.assign({}, DOMYSLNY); zastosuj(); zapisz(); });

  // przewodnik czytania — pasek śledzący kursor, tylko gdy tryb włączony
  var guide = (function () {
    var el = null, podpiete = false;
    function ruch(e) { if (el) el.style.top = (e.clientY - el.offsetHeight / 2) + 'px'; }
    return { hookup: function () {
      if (stan.guide) {
        if (!el) { el = document.createElement('div'); el.className = 'a11y-guide'; document.body.appendChild(el); }
        if (!podpiete) { document.addEventListener('mousemove', ruch); podpiete = true; }
      } else if (podpiete) { document.removeEventListener('mousemove', ruch); podpiete = false; }
    } };
  })();

  /* otwieranie / zamykanie z obsługą klawiatury */
  function otwarte() { return panel.hasAttribute('data-open'); }
  function otworz() {
    panel.setAttribute('data-open', ''); fab.setAttribute('aria-expanded', 'true');
    document.addEventListener('keydown', naKlawisz);
    document.addEventListener('click', naZewnatrz, true);
    var f = panel.querySelector('button'); if (f) f.focus();
  }
  function zamknij(oddaj) {
    panel.removeAttribute('data-open'); fab.setAttribute('aria-expanded', 'false');
    document.removeEventListener('keydown', naKlawisz);
    document.removeEventListener('click', naZewnatrz, true);
    if (oddaj !== false) fab.focus();
  }
  function naKlawisz(e) {
    if (e.key === 'Escape') { e.preventDefault(); zamknij(); return; }
    if (e.key === 'Tab') {
      var f = Array.prototype.slice.call(panel.querySelectorAll('button:not([disabled])'));
      if (!f.length) return;
      var a = f[0], z = f[f.length - 1];
      if (e.shiftKey && document.activeElement === a) { e.preventDefault(); z.focus(); }
      else if (!e.shiftKey && document.activeElement === z) { e.preventDefault(); a.focus(); }
    }
  }
  function naZewnatrz(e) { if (!panel.contains(e.target) && !fab.contains(e.target)) zamknij(false); }
  fab.addEventListener('click', function () { otwarte() ? zamknij() : otworz(); });
  var x = panel.querySelector('[data-a11y-close]');
  if (x) x.addEventListener('click', function () { zamknij(); });

  zastosuj();
})();

/* ---------- akordeon treści usługi (płynny, bez skakania) ----------
   Panel animuje wysokość przez grid-template-rows 0fr→1fr (klasa .is-open).
   Bez JS panel jest otwarty (CSS html:not(.js)) — treść nigdy nie znika. */
(function () {
  var sums = document.querySelectorAll('.sufold__sum');
  if (!sums.length) return;
  sums.forEach(function (btn) {
    btn.addEventListener('click', function () {
      var fold = btn.closest('.sufold');
      var open = fold.classList.toggle('is-open');
      btn.setAttribute('aria-expanded', open ? 'true' : 'false');
    });
  });
})();
